# Employee-Salary-Computation-System
Takes each employee's name, basic salary, HRA%, DA%, and PF% from the user Converts every input into the correct data type. Calculates salary components using arithmetic operations. Uses logical operators to categorize the salary range Prints a clean salary report for each employee.(report.txt)
